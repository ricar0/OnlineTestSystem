<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kaka.mapper.UserMapper">
    <select id="listUser" resultType="com.kaka.entity.User">
        select * from sys_user;
    </select>
    <select id="listUserByName" parameterType="String" resultType="com.kaka.entity.User">
        select * from sys_user where username = #{name};
    </select>
    <insert id="addUser" parameterType="com.kaka.entity.User">
        insert into sys_user value(null, #{username}, #{password});
    </insert>
</mapper>
