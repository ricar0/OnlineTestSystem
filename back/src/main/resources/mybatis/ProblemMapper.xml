<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kaka.mapper.ProblemMapper">
    <select id="getAll" resultType="com.kaka.entity.Problem">
        select * from problems_set;
    </select>
<<<<<<< HEAD
    <select id="getProblemByFilter" resultType="com.kaka.entity.Problem" parameterType="com.kaka.entity.ProblemFilter">
        SELECT * FROM problems_set
        <where>
            <if test="sourceList.size() > 0">
                AND source IN
                <foreach item="source" collection="sourceList" open="(" separator="," close=")">
                    #{source}
                </foreach>
            </if>
            <if test="difficultyList.size() > 0">
                AND difficulty IN
                <foreach item="difficulty" collection="difficultyList" open="(" separator="," close=")">
                    #{difficulty}
                </foreach>
            </if>
            <if test="labelList.size() > 0">
                AND label IN
                <foreach item="label" collection="labelList" open="(" separator="," close=")">
                    #{label}
                </foreach>
            </if>
        </where>
=======
    <select id="getProblemByFilter1" resultType="com.kaka.entity.Problem" parameterType="String">
        select * from problems_set where source = #{source} and difficulty = #{difficulty};
    </select>

    <select id="getProblemByFilter2" resultType="com.kaka.entity.Problem" parameterType="String">
        select * from problems_set where difficulty = #{difficulty};
    </select>

    <select id="getProblemByFilter3" resultType="com.kaka.entity.Problem" parameterType="String">
        select * from problems_set where source = #{source};
>>>>>>> d05ed3986ccc16b970daa509d36c355f343d6d85
    </select>
</mapper>